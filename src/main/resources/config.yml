# Deprecated as of v0.6.0.
# Snarky Server now uses split configuration files in the plugin data folder:
# - messages.yml
# - chances.yml
# - triggers.yml
#
# Existing servers are migrated automatically when possible.
# This resource remains only for migration reference and is no longer loaded at runtime.
